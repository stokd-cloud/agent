# auto-install-peers 保持 pnpm 默认（true）——pnpm-lock.yaml 的 settings
# 段记录的就是 true，此处若写 false，pnpm 10 会在 --frozen-lockfile 下以
# ERR_PNPM_LOCKFILE_CONFIG_MISMATCH 拒绝安装（CI 的 pnpm 11 不校验此项，
# 只在本地暴露）。锁文件是权威，配置与之对齐。
legacy-peer-deps=true
