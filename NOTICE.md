
# Notices and provenance

This repository is a public fork derived from `ccch1mneyyy/dsh-TUI` at DST v0.9.3, commit `a3439a3c7d7e7b3c9cfc505e833525376e8558d0`.

The retained donor package, its original notices, test sources, and pinned vendored build inputs live under `packages/tui`. The donor is MIT licensed; the original license remains at the repository root and is also recorded in `provenance/donor.json`. Dependency notices are preserved at `packages/tui/THIRD_PARTY_LICENSES`.

New Cloud Agents packages are distinct APIs. They are not represented as drop-in replacements for the donor or for Mono’s legacy agent package, and this MVP does not publish a general-purpose SDK.
